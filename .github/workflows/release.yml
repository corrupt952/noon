name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: bun-linux-x64
            artifact: noon-linux-x64
          - os: ubuntu-latest
            target: bun-linux-arm64
            artifact: noon-linux-arm64
          - os: macos-latest
            target: bun-darwin-x64
            artifact: noon-darwin-x64
          - os: macos-latest
            target: bun-darwin-arm64
            artifact: noon-darwin-arm64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build binary
        run: |
          bun build --compile src/index.ts \
            --outfile ${{ matrix.artifact }} \
            --target ${{ matrix.target }} \
            --define "PKG_VERSION='$(jq -r .version package.json)'"

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/noon-linux-x64/noon-linux-x64
            artifacts/noon-linux-arm64/noon-linux-arm64
            artifacts/noon-darwin-x64/noon-darwin-x64
            artifacts/noon-darwin-arm64/noon-darwin-arm64
